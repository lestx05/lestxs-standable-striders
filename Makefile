# Optional helper file
# Usage examples:
#   git init
#   git add .
#   git commit -m "Initial repository structure"

init:
	git init

first-commit:
	git add .
	git commit -m "Initial repository structure"
